{{- if .Values.bucket.enabled }}
apiVersion: storage.cnrm.cloud.google.com/v1beta1
kind: StorageBucket
metadata:
  name: {{ include "inventory.fullname" . }}
  annotations:
    cnrm.cloud.google.com/project-id: {{ .Values.projectId }}
spec:
  resourceID: {{ .Values.bucket.name }}
{{- end }}
